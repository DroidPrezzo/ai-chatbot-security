# ─── Direct dependencies ─────────────────────────────────────────
fastapi>=0.136.3
uvicorn[standard]>=0.32.0
httpx>=0.27.0
pydantic>=2.0.0
pyrit>=0.14.0

# ─── Security-pinned transitive dependencies ─────────────────────
# Pulled in by fastapi/pyrit but pinned here to patched versions to
# clear known CVEs (verified with `pip-audit`). Review periodically.
starlette>=1.3.1        # CVE-2026-54282 / CVE-2026-54283
aiohttp>=3.14.1         # CVE-2026-54273..54280 (via pyrit)
cryptography>=48.0.1    # GHSA-537c-gmf6-5ccf (via pyrit)
pypdf>=6.13.3           # GHSA-jm82-fx9c-mx94 (via pyrit)
